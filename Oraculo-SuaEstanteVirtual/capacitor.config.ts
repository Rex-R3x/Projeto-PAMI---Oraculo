import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Oraculo-SuaEstanteVirtual',
  webDir: 'www'
};

export default config;
